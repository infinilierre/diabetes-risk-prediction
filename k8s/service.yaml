apiVersion: v1
kind: Service
metadata:
  name: diabetes-service
spec:
  type: LoadBalancer # Dışarıdan erişim sağlamak için
  selector:
    app: diabetes-prediction
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80

      